import { BrowserRouter as Router, Routes, Route } from "react-router-dom";

export default function SetupWalletRoutes() {
  return (
    <Router>
      <Routes>
        <Route path="/setupWallet/" element={} />
      </Routes>
    </Router>
  );
}
